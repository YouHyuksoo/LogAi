# ============================================
# LogAi 환경 변수 설정 예시
# 이 파일을 .env로 복사한 후 실제 값을 입력하세요
# cp .env.example .env (Linux/Mac)
# copy .env.example .env (Windows)
# ============================================

# ==================== AI Model Config ====================
HF_TOKEN=your_huggingface_token_here
LLM_MODEL_NAME=meta-llama/Meta-Llama-3.1-8B-Instruct

# LLM Provider: local | openai | gemini | mistral
LLM_PROVIDER=mistral

# OpenAI API (LLM_PROVIDER=openai 사용 시)
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-mini

# Google Gemini API (LLM_PROVIDER=gemini 사용 시)
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.5-flash-lite

# Mistral AI API (LLM_PROVIDER=mistral 사용 시)
MISTRAL_API_KEY=your_mistral_api_key_here
MISTRAL_MODEL=mistral-large-latest

# ==================== Embedding Config ====================
# Embedding Provider: local-gpu | local-cpu | openai
EMBEDDING_PROVIDER=local-cpu

# Local GPU Embedding (EMBEDDING_PROVIDER=local-gpu 사용 시)
TEI_URL=http://localhost:8080
TEI_MODEL=BAAI/bge-m3

# Local CPU Embedding (EMBEDDING_PROVIDER=local-cpu 사용 시)
CPU_EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# OpenAI Embedding (EMBEDDING_PROVIDER=openai 사용 시)
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# ==================== Infrastructure Hosts ====================
# 로컬 개발용
QDRANT_HOST=localhost
CLICKHOUSE_HOST=localhost
REDPANDA_BROKER=localhost:29092

# Docker 프로덕션용 (docker-compose.prod.yml 사용 시)
# QDRANT_HOST=qdrant
# CLICKHOUSE_HOST=clickhouse
# REDPANDA_BROKER=redpanda:9092

# ==================== Ports ====================
QDRANT_PORT=6333
CLICKHOUSE_PORT=9000
REDPANDA_PORT=8082
VLLM_PORT=8000
TEI_PORT=8080

# ==================== Vector Classification ====================
VECTOR_CLASSIFICATION_ENABLED=true
VECTOR_CLASSIFICATION_THRESHOLD_NORMAL=0.7
VECTOR_CLASSIFICATION_THRESHOLD_ANOMALY=0.8
VECTOR_TOP_K_SEARCH=3

# ==================== App Settings (Frontend에서 수정 가능) ====================
# 이 값들은 설정 페이지에서 변경하면 자동으로 업데이트됩니다
ANOMALY_THRESHOLD=0.7
THEME=dark
NOTIFICATIONS_ENABLED=true
AUTO_REFRESH=true
REFRESH_INTERVAL=30
LOG_STORAGE_POLICY=all
