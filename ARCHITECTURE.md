# LogAi 시스템 아키텍처 문서

> **버전**: 1.0.0
> **최종 업데이트**: 2026-01-03
> **작성자**: Claude AI Assistant

---

## 📋 목차

1. [시스템 개요](#1-시스템-개요)
2. [전체 아키텍처 다이어그램](#2-전체-아키텍처-다이어그램)
3. [기술 스택](#3-기술-스택)
4. [인프라 레이어](#4-인프라-레이어)
5. [Backend 아키텍처](#5-backend-아키텍처)
6. [Frontend 아키텍처](#6-frontend-아키텍처)
7. [데이터 흐름](#7-데이터-흐름)
8. [AI 파이프라인](#8-ai-파이프라인)
9. [API 명세](#9-api-명세)
10. [배포 구성](#10-배포-구성)
11. [보안 고려사항](#11-보안-고려사항)
12. [확장성 및 성능](#12-확장성-및-성능)

---

## 1. 시스템 개요

### 1.1 프로젝트 소개

**LogAi**는 **온프레미스 자율형 로그 분석 시스템**입니다. 대규모 인프라에서 발생하는 로그를 실시간으로 수집, 분석하여 이상 징후를 감지하고, AI 기반으로 장애 원인을 분석하여 자동 보고하는 엔터프라이즈급 SRE(Site Reliability Engineering) 솔루션입니다.

### 1.2 핵심 목표

| 목표 | 설명 |
|------|------|
| **실시간 이상 탐지** | 초당 수천 건의 로그를 처리하며 이상 패턴 즉시 감지 |
| **자동 근본 원인 분석** | RAG + LLM 기반 장애 원인 자동 분석 |
| **MTTR 최소화** | 장애 발생 시 Mean Time To Recovery 단축 |
| **온프레미스 AI** | 데이터 외부 유출 없이 로컬 GPU에서 AI 추론 |
| **확장 가능한 구조** | 마이크로서비스 기반 수평 확장 지원 |

### 1.3 핵심 기능

```
┌─────────────────────────────────────────────────────────────────┐
│                        LogAi 핵심 기능                           │
├─────────────────────────────────────────────────────────────────┤
│  🔍 로그 수집          │  Rust 기반 Vector로 고성능 로그 수집     │
│  📊 템플릿 추출        │  Drain3 알고리즘으로 로그 패턴 자동 분류  │
│  🚨 이상 탐지          │  PyOD (Isolation Forest)로 비정상 감지   │
│  🤖 AI 분석            │  vLLM + RAG로 장애 원인 자동 분석        │
│  📢 자동 알림          │  Slack Webhook으로 실시간 알림 발송       │
│  📈 대시보드           │  Next.js 기반 실시간 모니터링 UI         │
└─────────────────────────────────────────────────────────────────┘
```

---

## 2. 전체 아키텍처 다이어그램

### 2.1 시스템 전체 구성도

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              LogAi System Architecture                       │
└─────────────────────────────────────────────────────────────────────────────┘

                              ┌──────────────────┐
                              │   Log Sources    │
                              │  (Apps, Servers) │
                              └────────┬─────────┘
                                       │
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                            DATA INGESTION LAYER                              │
│  ┌─────────────┐    ┌─────────────────┐    ┌─────────────────────────────┐  │
│  │   Vector    │───▶│    Redpanda     │───▶│    Ingest Consumer          │  │
│  │ (Collector) │    │ (Message Queue) │    │    (Python Worker)          │  │
│  │  :N/A       │    │    :29092       │    │                             │  │
│  └─────────────┘    └─────────────────┘    │  ┌─────────┐ ┌───────────┐  │  │
│                                             │  │ Drain3  │ │   PyOD    │  │  │
│                                             │  │ Parser  │ │ Detector  │  │  │
│                                             │  └────┬────┘ └─────┬─────┘  │  │
│                                             └──────┼─────────────┼────────┘  │
└─────────────────────────────────────────────────────┼─────────────┼──────────┘
                                                      │             │
                                                      ▼             ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                             DATA STORAGE LAYER                               │
│  ┌────────────────────────────┐    ┌──────────────────────────────────────┐ │
│  │       ClickHouse           │    │              Qdrant                  │ │
│  │     (OLAP Database)        │    │         (Vector Database)            │ │
│  │         :8123              │    │            :6333                     │ │
│  │                            │    │                                      │ │
│  │  ┌──────────┐ ┌─────────┐  │    │  ┌────────────────┐                  │ │
│  │  │   logs   │ │anomalies│  │    │  │incident_manuals│                  │ │
│  │  │  Table   │ │  Table  │  │    │  │  Collection    │                  │ │
│  │  └──────────┘ └─────────┘  │    │  └────────────────┘                  │ │
│  └────────────────────────────┘    └──────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────────┘
                                       │
                     ┌─────────────────┴─────────────────┐
                     ▼                                   ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                              AI INFERENCE LAYER                              │
│  ┌──────────────────────────┐    ┌──────────────────────────────────────┐   │
│  │          vLLM            │    │               TEI                    │   │
│  │    (LLM Inference)       │    │      (Text Embedding Inference)      │   │
│  │        :8000             │    │             :8080                    │   │
│  │                          │    │                                      │   │
│  │  ┌────────────────────┐  │    │  ┌────────────────────────────────┐  │   │
│  │  │ Llama 3.1-8B-Inst  │  │    │  │         BAAI/bge-m3            │  │   │
│  │  │    (GPU: CUDA)     │  │    │  │        (Embedding Model)       │  │   │
│  │  └────────────────────┘  │    │  └────────────────────────────────┘  │   │
│  └──────────────────────────┘    └──────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
                                       │
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           APPLICATION LAYER                                  │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │                         FastAPI Backend (:8000)                        │ │
│  │  ┌──────────────────────────────────────────────────────────────────┐  │ │
│  │  │                       LangGraph Agent                            │  │ │
│  │  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────┐   │  │ │
│  │  │  │retrieve_info │─▶│   analyze    │─▶│     notify_admin     │   │  │ │
│  │  │  │  (RAG Node)  │  │(vLLM Node)   │  │   (Slack Node)       │   │  │ │
│  │  │  └──────────────┘  └──────────────┘  └──────────────────────┘   │  │ │
│  │  └──────────────────────────────────────────────────────────────────┘  │ │
│  │                                                                        │ │
│  │  ┌────────────────┐ ┌────────────────┐ ┌──────────────────────────┐   │ │
│  │  │  /api/v1/logs  │ │ /api/v1/stats │ │    /api/v1/analysis      │   │ │
│  │  └────────────────┘ └────────────────┘ └──────────────────────────┘   │ │
│  │  ┌──────────────────────────────────────────────────────────────────┐  │ │
│  │  │                      /api/v1/chat (RAG + vLLM)                   │  │ │
│  │  └──────────────────────────────────────────────────────────────────┘  │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────────┘
                                       │
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          PRESENTATION LAYER                                  │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │                     Next.js 14 Frontend (:3000)                        │ │
│  │  ┌───────────────┐  ┌───────────────┐  ┌─────────────────────────────┐ │ │
│  │  │   Dashboard   │  │   AI Chat     │  │         Settings            │ │ │
│  │  │   (page.tsx)  │  │  (/chat)      │  │        (/settings)          │ │ │
│  │  │               │  │               │  │                             │ │ │
│  │  │ • Stats Cards │  │ • RAG Chat    │  │ • LLM Provider Switch       │ │ │
│  │  │ • Charts      │  │ • History     │  │ • Threshold Config          │ │ │
│  │  │ • Log Stream  │  │ • Markdown    │  │ • API Key Management        │ │ │
│  │  └───────────────┘  └───────────────┘  └─────────────────────────────┘ │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────────┘
                                       │
                                       ▼
                              ┌──────────────────┐
                              │   Slack Alert    │
                              │   (Webhook)      │
                              └──────────────────┘
```

### 2.2 컴포넌트 관계도

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          Component Relationships                         │
└─────────────────────────────────────────────────────────────────────────┘

  ┌─────────────┐        ┌─────────────┐        ┌─────────────┐
  │   Vector    │ ─────▶ │  Redpanda   │ ─────▶ │  Consumer   │
  │ (Collector) │  JSON  │   (Kafka)   │  Topic │  (Python)   │
  └─────────────┘        └─────────────┘        └──────┬──────┘
                                                       │
                         ┌─────────────────────────────┼─────────────────┐
                         │                             │                 │
                         ▼                             ▼                 ▼
                  ┌─────────────┐            ┌─────────────┐    ┌─────────────┐
                  │   Drain3    │            │ ClickHouse  │    │    PyOD     │
                  │  (Parser)   │            │  (Storage)  │    │ (Detector)  │
                  └──────┬──────┘            └──────┬──────┘    └──────┬──────┘
                         │                          │                  │
                         └──────────────┬───────────┘                  │
                                        │                              │
                                        ▼                              │
                               ┌─────────────────┐                     │
                               │ ClickHouse DB   │ ◀───────────────────┘
                               │ • logs          │    (Anomaly Insert)
                               │ • anomalies     │
                               └────────┬────────┘
                                        │
                    ┌───────────────────┼───────────────────┐
                    │                   │                   │
                    ▼                   ▼                   ▼
             ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
             │   FastAPI   │    │  RAG Engine │    │   Qdrant    │
             │ (REST API)  │◀──▶│   (Search)  │◀──▶│  (VectorDB) │
             └──────┬──────┘    └──────┬──────┘    └─────────────┘
                    │                  │                   ▲
                    │                  │                   │
                    │                  ▼                   │
                    │           ┌─────────────┐            │
                    │           │     TEI     │────────────┘
                    │           │ (Embedding) │   (Query Embed)
                    │           └─────────────┘
                    │
                    ▼
             ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
             │  LangGraph  │───▶│    vLLM     │───▶│   Notifier  │
             │   (Agent)   │    │   (LLM)     │    │  (Slack)    │
             └─────────────┘    └─────────────┘    └─────────────┘
```

---

## 3. 기술 스택

### 3.1 기술 스택 요약

| 레이어 | 기술 | 버전 | 용도 |
|--------|------|------|------|
| **Frontend** | Next.js | 14.1.0 | App Router 기반 React 프레임워크 |
| | React | 18.x | UI 라이브러리 |
| | TypeScript | 5.x | 타입 안전성 |
| | Tailwind CSS | 3.3.0 | 유틸리티 기반 스타일링 |
| | Recharts | 2.12.2 | 데이터 시각화 차트 |
| | Lucide React | 0.344.0 | 아이콘 라이브러리 |
| | Framer Motion | 11.0.8 | 애니메이션 |
| **Backend** | FastAPI | 0.109.0 | 비동기 Python 웹 프레임워크 |
| | Pydantic | 2.5.3 | 데이터 검증 |
| | LangGraph | 0.0.20 | AI 에이전트 워크플로우 |
| | LangChain | 0.1.0 | LLM 통합 프레임워크 |
| | OpenAI SDK | 1.10.0 | vLLM/OpenAI API 클라이언트 |
| **AI/ML** | vLLM | latest | GPU 가속 LLM 추론 엔진 |
| | TEI | 1.2 | GPU 가속 임베딩 추론 |
| | Llama 3.1-8B | Instruct | 한국어 지원 오픈소스 LLM |
| | BAAI/bge-m3 | - | 다국어 임베딩 모델 |
| | Drain3 | 0.9.11 | 로그 템플릿 추출 알고리즘 |
| | PyOD | 1.1.3 | 이상 탐지 알고리즘 모음 |
| **Database** | ClickHouse | 24.1 | 컬럼 기반 OLAP 데이터베이스 |
| | Qdrant | 1.7.4 | 벡터 유사도 검색 DB |
| **Message Queue** | Redpanda | 23.3.4 | Kafka 호환 스트리밍 플랫폼 |
| **Log Collector** | Vector | 0.36.0 | Rust 기반 고성능 로그 수집기 |
| **Infrastructure** | Docker Compose | 3.8 | 컨테이너 오케스트레이션 |
| | NVIDIA Runtime | - | GPU 컨테이너 지원 |

### 3.2 기술 선택 이유

#### 3.2.1 Redpanda vs Apache Kafka
```
┌─────────────────────────────────────────────────────────────────┐
│                    Redpanda 선택 이유                            │
├─────────────────────────────────────────────────────────────────┤
│ ✅ Kafka API 100% 호환 (기존 클라이언트 그대로 사용)             │
│ ✅ JVM 불필요 (C++ 네이티브 → 낮은 메모리 사용량)                │
│ ✅ 단일 바이너리 (ZooKeeper 불필요 → 운영 단순화)                │
│ ✅ 낮은 지연시간 (P99 < 10ms)                                   │
│ ✅ 온프레미스 최적화 (리소스 효율적)                             │
└─────────────────────────────────────────────────────────────────┘
```

#### 3.2.2 ClickHouse vs PostgreSQL/TimescaleDB
```
┌─────────────────────────────────────────────────────────────────┐
│                   ClickHouse 선택 이유                           │
├─────────────────────────────────────────────────────────────────┤
│ ✅ 컬럼 기반 스토리지 → 로그 분석 쿼리 100배 빠름                │
│ ✅ 압축률 10:1 이상 (저장 비용 절감)                             │
│ ✅ 초당 수백만 행 삽입 가능                                      │
│ ✅ SQL 지원 (러닝 커브 낮음)                                     │
│ ✅ 시계열 데이터 파티셔닝 네이티브 지원                          │
└─────────────────────────────────────────────────────────────────┘
```

#### 3.2.3 vLLM vs Ollama vs llama.cpp
```
┌─────────────────────────────────────────────────────────────────┐
│                     vLLM 선택 이유                               │
├─────────────────────────────────────────────────────────────────┤
│ ✅ PagedAttention → GPU 메모리 효율 2-4배 향상                   │
│ ✅ Continuous Batching → 높은 처리량                             │
│ ✅ OpenAI API 호환 → 기존 코드 재사용                            │
│ ✅ 엔터프라이즈급 안정성                                         │
│ ✅ 다중 GPU 분산 추론 지원                                       │
└─────────────────────────────────────────────────────────────────┘
```

---

## 4. 인프라 레이어

### 4.1 Docker 서비스 구성

```yaml
# docker-compose.yml 서비스 목록
┌─────────────────────────────────────────────────────────────────┐
│  Service      │  Port(s)           │  Description              │
├───────────────┼────────────────────┼───────────────────────────┤
│  redpanda     │  29092, 8082, 9644 │  Kafka 호환 메시지 브로커  │
│  clickhouse   │  8123, 9000        │  OLAP 데이터베이스        │
│  qdrant       │  6333, 6334        │  벡터 데이터베이스        │
│  vector       │  N/A               │  로그 수집 에이전트       │
│  backend      │  8000              │  FastAPI 서버            │
│  frontend     │  3000              │  Next.js 서버            │
└───────────────┴────────────────────┴───────────────────────────┘

# docker-compose.ai.yml 서비스 목록 (GPU 필요)
┌─────────────────────────────────────────────────────────────────┐
│  Service      │  Port              │  Description              │
├───────────────┼────────────────────┼───────────────────────────┤
│  vllm         │  8000              │  LLM 추론 엔진 (GPU)      │
│  tei          │  8080              │  임베딩 추론 엔진 (GPU)   │
└───────────────┴────────────────────┴───────────────────────────┘
```

### 4.2 네트워크 토폴로지

```
┌─────────────────────────────────────────────────────────────────┐
│                      Docker Network: logai-net                   │
│                         (bridge driver)                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│   ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐     │
│   │ vector  │    │redpanda │    │clickhouse│   │ qdrant  │     │
│   └────┬────┘    └────┬────┘    └────┬────┘    └────┬────┘     │
│        │              │              │              │           │
│        └──────────────┴──────┬───────┴──────────────┘           │
│                              │                                   │
│                     Internal Network                             │
│                              │                                   │
│        ┌─────────────────────┼─────────────────────┐            │
│        │                     │                     │            │
│   ┌────┴────┐          ┌─────┴─────┐         ┌─────┴─────┐     │
│   │ backend │          │   vllm    │         │    tei    │     │
│   │ :8000   │          │  :8000*   │         │   :8080   │     │
│   └────┬────┘          └───────────┘         └───────────┘     │
│        │                                                        │
│   ┌────┴────┐                                                   │
│   │frontend │                                                   │
│   │ :3000   │                                                   │
│   └─────────┘                                                   │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
                              │
                    External Access (Host)
                              │
              ┌───────────────┴───────────────┐
              │                               │
         localhost:3000               localhost:8000
          (Frontend)                   (Backend API)
```

### 4.3 볼륨 마운트

```
┌─────────────────────────────────────────────────────────────────┐
│                        Volume Mounts                             │
├─────────────────────────────────────────────────────────────────┤
│  Host Path                    │  Container Path                 │
├───────────────────────────────┼─────────────────────────────────┤
│  ./data/redpanda              │  /var/lib/redpanda/data        │
│  ./data/clickhouse            │  /var/lib/clickhouse           │
│  ./data/qdrant                │  /qdrant/storage               │
│  ./data/vector                │  /var/lib/vector               │
│  ./data/tei                   │  /data                         │
│  ./config/vector.toml         │  /etc/vector/vector.toml       │
│  ./config/clickhouse_users.xml│  /etc/clickhouse-server/...    │
│  ~/.cache/huggingface         │  /root/.cache/huggingface      │
│  ./backend                    │  /app (dev mount)              │
└───────────────────────────────┴─────────────────────────────────┘
```

---

## 5. Backend 아키텍처

### 5.1 디렉토리 구조

```
backend/
├── main.py                      # FastAPI 앱 진입점
├── requirements.txt             # Python 의존성
├── Dockerfile                   # 컨테이너 빌드 정의
├── drain3.ini                   # Drain3 파서 설정
│
└── app/
    ├── core/                    # 핵심 설정 및 상수
    │   ├── config.py            # 환경 변수 설정 클래스
    │   └── system_prompt.md     # AI 페르소나 프롬프트 (한국어)
    │
    ├── api/                     # REST API 레이어
    │   └── api_v1/
    │       ├── api.py           # 라우터 통합
    │       └── endpoints/       # 개별 엔드포인트
    │           ├── logs.py      # GET /logs - 로그 조회
    │           ├── stats.py     # GET /stats - 통계 조회
    │           ├── analysis.py  # POST /analysis - 분석 트리거
    │           └── chat.py      # POST /chat - AI 채팅
    │
    └── services/                # 비즈니스 로직 레이어
        ├── ingest_consumer.py   # Kafka 메시지 소비 워커
        ├── drain_parser.py      # 로그 템플릿 추출
        ├── anomaly_detector.py  # 이상 탐지 (PyOD)
        ├── clickhouse_client.py # ClickHouse DB 클라이언트
        ├── embedding_client.py  # TEI 임베딩 클라이언트
        ├── rag_engine.py        # RAG 검색 엔진
        ├── llm_factory.py       # LLM 클라이언트 팩토리
        ├── agent_graph.py       # LangGraph 에이전트 워크플로우
        └── notifier.py          # Slack 알림 발송
```

### 5.2 서비스 계층 상세

#### 5.2.1 ingest_consumer.py - 로그 수집 워커

```python
"""
데이터 흐름:
Redpanda (logs-raw topic)
    │
    ▼
KafkaConsumer (batch 100개씩)
    │
    ├──▶ drain_parser.parse()  →  템플릿 ID, 템플릿 문자열, 파라미터 추출
    │
    └──▶ clickhouse_client.insert_logs()  →  ClickHouse에 배치 삽입

특징:
- auto_offset_reset='latest' (실시간 로그만 처리)
- 100개 배치 또는 1초마다 flush
- 서비스 분리 가능 (별도 프로세스로 실행)
"""
```

#### 5.2.2 drain_parser.py - Drain3 로그 파서

```python
"""
Drain3 알고리즘 동작:
1. 로그 메시지를 토큰으로 분리
2. 고정 깊이 트리에서 유사 로그 클러스터 검색
3. 유사도 임계값(0.5) 이상이면 기존 템플릿에 병합
4. 그렇지 않으면 새 템플릿 생성

입력:  "[2024-01-01 10:00:00] ERROR Connection to 192.168.1.1 failed"
출력:  {
         "template_id": 42,
         "template": "[<*>] ERROR Connection to <*> failed",
         "params": ["2024-01-01 10:00:00", "192.168.1.1"]
       }
"""
```

#### 5.2.3 anomaly_detector.py - 이상 탐지

```python
"""
PyOD Isolation Forest 동작:
1. 최근 24시간 로그 템플릿 빈도 수집
2. 특성 벡터: 템플릿별 분당 발생 횟수
3. 모델 학습: contamination=0.01 (1%를 이상치로 가정)
4. 예측: 최근 5분 데이터로 이상 탐지

이상 탐지 시:
- anomalies 테이블에 기록
- (TODO) LangGraph 에이전트 트리거
"""
```

#### 5.2.4 agent_graph.py - LangGraph 워크플로우

```
┌─────────────────────────────────────────────────────────────────┐
│                    LangGraph StateGraph                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  AgentState (TypedDict):                                        │
│  ├─ anomaly_data: dict      # 입력: 이상 징후 정보              │
│  ├─ log_context: str        # ClickHouse에서 조회한 주변 로그   │
│  ├─ manual_context: list    # Qdrant에서 검색한 유사 매뉴얼     │
│  ├─ analysis_result: str    # vLLM 분석 결과                    │
│  └─ is_critical: bool       # 심각도 판단                       │
│                                                                  │
│  Workflow:                                                       │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐  │
│  │  retrieve_info  │─▶│     analyze     │─▶│  notify_admin   │  │
│  │                 │  │                 │  │                 │  │
│  │ • ClickHouse    │  │ • system_prompt │  │ • Slack Webhook │  │
│  │   로그 조회     │  │ • vLLM 호출     │  │ • 알림 발송     │  │
│  │ • Qdrant 검색   │  │ • 분석 생성     │  │                 │  │
│  └─────────────────┘  └─────────────────┘  └─────────────────┘  │
│         │                                            │          │
│         └────────────────────────────────────────────┘          │
│                              ▼                                   │
│                            [END]                                 │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

#### 5.2.5 rag_engine.py - RAG 검색 엔진

```python
"""
RAG (Retrieval-Augmented Generation) 흐름:

1. 쿼리 임베딩 생성
   ┌────────────────┐    ┌────────────────┐
   │  "메모리 누수" │───▶│      TEI       │───▶ [0.12, -0.34, ...]
   │   (쿼리)      │    │   /embed API   │     (1024 차원 벡터)
   └────────────────┘    └────────────────┘

2. Qdrant 유사도 검색
   ┌────────────────────┐    ┌────────────────────┐
   │  쿼리 벡터         │───▶│      Qdrant        │
   │  [0.12, -0.34, ..]│    │ incident_manuals   │
   └────────────────────┘    │ collection         │
                              └─────────┬──────────┘
                                        │
                                        ▼
                              Top-K (3개) 유사 문서 반환

3. ClickHouse 시간 문맥 검색
   ┌────────────────────┐    ┌────────────────────┐
   │  이상 발생 시간    │───▶│    ClickHouse      │
   │  ±5분 윈도우      │    │     logs 테이블    │
   └────────────────────┘    └─────────┬──────────┘
                                        │
                                        ▼
                              시간순 정렬된 로그 100개 반환
"""
```

### 5.3 데이터베이스 스키마

#### 5.3.1 ClickHouse 스키마

```sql
-- 로그 테이블 (시계열 데이터)
CREATE TABLE logs (
    timestamp    DateTime,           -- 로그 발생 시간
    log_level    LowCardinality(String),  -- INFO, WARN, ERROR 등
    service      String,             -- 서비스명 (auth-service, payment-api 등)
    template_id  UInt16,             -- Drain3 템플릿 ID
    log_template String,             -- Drain3 추출 템플릿
    raw_message  String,             -- 원본 로그 메시지
    parameters   Array(String)       -- 추출된 변수 값들
) ENGINE = MergeTree()
ORDER BY timestamp;

-- 이상 탐지 결과 테이블
CREATE TABLE anomalies (
    timestamp     DateTime,          -- 탐지 시간
    template_id   UInt16,            -- 관련 템플릿 ID
    anomaly_score Float32,           -- 이상 점수 (Isolation Forest)
    is_anomaly    UInt8,             -- 이상 여부 (0/1)
    details       String             -- 상세 정보 (JSON 또는 텍스트)
) ENGINE = MergeTree()
ORDER BY timestamp;
```

#### 5.3.2 Qdrant 컬렉션

```json
{
  "collection_name": "incident_manuals",
  "vectors_config": {
    "size": 1024,              // BAAI/bge-m3 임베딩 차원
    "distance": "Cosine"       // 코사인 유사도
  },
  "payload_schema": {
    "title": "string",         // 매뉴얼/사례 제목
    "content": "string",       // 본문 내용
    "category": "string",      // 장애 유형 (memory, network, disk 등)
    "created_at": "datetime"   // 생성 시간
  }
}
```

---

## 6. Frontend 아키텍처

### 6.1 디렉토리 구조

```
frontend/
├── app/                         # Next.js 14 App Router
│   ├── layout.tsx               # 루트 레이아웃 (Header + Sidebar)
│   ├── page.tsx                 # 메인 대시보드 (/)
│   ├── globals.css              # 전역 스타일 + Tailwind 설정
│   │
│   ├── chat/
│   │   └── page.tsx             # AI 채팅 페이지 (/chat)
│   │
│   └── settings/
│       └── page.tsx             # 설정 페이지 (/settings)
│
├── components/
│   └── layout/
│       ├── header.tsx           # 상단 헤더 (검색, 알림, 프로필)
│       └── sidebar.tsx          # 좌측 사이드바 (네비게이션)
│
├── lib/
│   └── utils.ts                 # cn() 헬퍼 (Tailwind 클래스 병합)
│
├── package.json                 # NPM 의존성
├── tailwind.config.ts           # Tailwind CSS 설정
├── tsconfig.json                # TypeScript 설정
└── next.config.mjs              # Next.js 설정
```

### 6.2 페이지 구성

```
┌─────────────────────────────────────────────────────────────────┐
│                        Page Structure                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  /  (Dashboard - page.tsx)                                      │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  Stats Cards (4개)                                          │ │
│  │  ├─ Anomaly Score: 현재 이상 점수                          │ │
│  │  ├─ Active Incidents: 활성 장애 수                         │ │
│  │  ├─ Log Volume: 초당 로그 수                               │ │
│  │  └─ AI Usage: GPU 사용률                                   │ │
│  ├────────────────────────────────────────────────────────────┤ │
│  │  Main Chart (Recharts LineChart)                           │ │
│  │  └─ 실시간 이상 탐지 트렌드                                │ │
│  ├────────────────────────────────────────────────────────────┤ │
│  │  Live Log Stream                                            │ │
│  │  └─ 최근 15개 로그 실시간 스트리밍                         │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  /chat  (AI Chat - chat/page.tsx)                               │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  Message List                                               │ │
│  │  ├─ User Message (오른쪽 정렬, 파란색)                     │ │
│  │  └─ Bot Response (왼쪽 정렬, 회색)                         │ │
│  ├────────────────────────────────────────────────────────────┤ │
│  │  Input Form                                                 │ │
│  │  └─ 텍스트 입력 + 전송 버튼                                │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  /settings  (Settings - settings/page.tsx)                      │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  LLM Provider Selection                                     │ │
│  │  ├─ On-Premise (vLLM) - 기본값                             │ │
│  │  └─ External API (OpenAI)                                  │ │
│  ├────────────────────────────────────────────────────────────┤ │
│  │  Anomaly Detection Threshold                                │ │
│  │  └─ 슬라이더 (0-100%)                                      │ │
│  ├────────────────────────────────────────────────────────────┤ │
│  │  API Key Input (OpenAI 선택 시)                             │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 6.3 컴포넌트 계층

```
Layout (layout.tsx)
├── Header (components/layout/header.tsx)
│   ├── Search Input
│   ├── Notification Bell
│   └── User Profile
│
├── Sidebar (components/layout/sidebar.tsx)
│   ├── Logo (LOG.AI)
│   ├── Operations Menu
│   │   ├── Monitoring (/)
│   │   ├── Analysis (/analysis) ← 미구현
│   │   └── AI Agent Chat (/chat)
│   ├── System Menu
│   │   ├── Infrastructure (/infra) ← 미구현
│   │   └── Settings (/settings)
│   └── System Status Badge
│
└── Main Content (children)
    └── [Page Component]
```

### 6.4 스타일링 시스템

```css
/* globals.css 커스텀 유틸리티 클래스 */

.glass-panel {
  /* 글라스모피즘 효과 */
  background: rgba(17, 24, 39, 0.8);
  backdrop-filter: blur(12px);
  border: 1px solid rgba(255, 255, 255, 0.1);
}

.neon-text {
  /* 네온 텍스트 글로우 */
  text-shadow: 0 0 10px currentColor,
               0 0 20px currentColor,
               0 0 40px currentColor;
}

.neon-border {
  /* 네온 보더 글로우 */
  box-shadow: 0 0 5px theme('colors.primary'),
              0 0 10px theme('colors.primary'),
              inset 0 0 5px theme('colors.primary');
}
```

---

## 7. 데이터 흐름

### 7.1 로그 수집 → 저장 흐름

```
┌─────────────────────────────────────────────────────────────────┐
│                    Log Ingestion Pipeline                        │
└─────────────────────────────────────────────────────────────────┘

Step 1: 로그 수집 (Vector)
┌─────────────────┐
│  Application    │   JSON 로그
│  Logs           │──────────────▶  Vector Agent
│  (stdout/file)  │                 (demo_logs source)
└─────────────────┘                      │
                                         │
Step 2: 메시지 큐잉 (Redpanda)           ▼
                              ┌─────────────────┐
                              │    Redpanda     │
                              │  Topic: logs-raw│
                              └────────┬────────┘
                                       │
Step 3: 소비 및 처리 (Python Worker)    ▼
                              ┌─────────────────┐
                              │ ingest_consumer │
                              │     .py         │
                              └────────┬────────┘
                                       │
                    ┌──────────────────┼──────────────────┐
                    │                  │                  │
                    ▼                  ▼                  ▼
Step 4: 파싱       Step 5: 배치 삽입  Step 6: (비동기) 이상 탐지
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│   Drain3    │    │ ClickHouse  │    │    PyOD     │
│   Parser    │    │  INSERT     │    │  Detector   │
└──────┬──────┘    └─────────────┘    └──────┬──────┘
       │                                      │
       ▼                                      ▼
  템플릿 추출                           이상 탐지 시
  - template_id                         anomalies 테이블 INSERT
  - template
  - parameters
```

### 7.2 이상 탐지 → AI 분석 흐름

```
┌─────────────────────────────────────────────────────────────────┐
│                   Anomaly → Analysis Pipeline                    │
└─────────────────────────────────────────────────────────────────┘

Step 1: 이상 탐지
┌─────────────────┐
│ AnomalyDetector │
│   (PyOD)        │───▶ is_anomaly == 1 ?
└─────────────────┘            │
                               │ Yes
                               ▼
Step 2: 에이전트 트리거
┌─────────────────────────────────────────────────────────────────┐
│                     LangGraph Agent                              │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │  Initial State:                                             ││
│  │  {                                                          ││
│  │    anomaly_data: { template_id, score, timestamp },         ││
│  │    log_context: "",                                         ││
│  │    manual_context: [],                                      ││
│  │    analysis_result: ""                                      ││
│  │  }                                                          ││
│  └─────────────────────────────────────────────────────────────┘│
│                               │                                  │
│                               ▼                                  │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐ │
│  │ retrieve_info   │  │     analyze     │  │  notify_admin   │ │
│  │                 │  │                 │  │                 │ │
│  │ ClickHouse:     │  │ vLLM:           │  │ Slack:          │ │
│  │ - 주변 로그 조회│  │ - system_prompt │  │ - 분석 결과     │ │
│  │                 │  │ - 문맥 + 질문   │  │   알림 발송     │ │
│  │ Qdrant:         │  │ - 응답 생성     │  │                 │ │
│  │ - 유사 매뉴얼   │  │                 │  │                 │ │
│  └────────┬────────┘  └────────┬────────┘  └────────┬────────┘ │
│           │                    │                    │          │
│           └────────────────────┴────────────────────┘          │
│                               │                                  │
│                               ▼                                  │
│                     Final State:                                 │
│                     { analysis_result: "### 장애 분석..." }     │
└─────────────────────────────────────────────────────────────────┘
```

### 7.3 사용자 채팅 흐름

```
┌─────────────────────────────────────────────────────────────────┐
│                      Chat API Pipeline                           │
└─────────────────────────────────────────────────────────────────┘

Step 1: 사용자 입력
┌─────────────────┐
│   Frontend      │
│   /chat         │───▶ POST /api/v1/chat
└─────────────────┘      { message: "최근 장애 원인은?" }
                                    │
                                    ▼
Step 2: RAG 검색
┌─────────────────────────────────────────────────────────────────┐
│                        RAG Engine                                │
│                                                                  │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────┐         │
│  │    TEI      │───▶│   Qdrant    │───▶│ Top-3 Docs  │         │
│  │  (Embed)    │    │  (Search)   │    │             │         │
│  └─────────────┘    └─────────────┘    └─────────────┘         │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
Step 3: LLM 추론
┌─────────────────────────────────────────────────────────────────┐
│                         vLLM                                     │
│                                                                  │
│  Messages:                                                       │
│  [                                                               │
│    { role: "system", content: system_prompt.md },               │
│    { role: "user", content: "### 참조 문서:\n..." + 질문 }      │
│  ]                                                               │
│                                                                  │
│  Response:                                                       │
│  "### 분석 결과\n메모리 누수가 의심됩니다..."                   │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
Step 4: 응답 반환
┌─────────────────┐
│   Frontend      │◀─── { response: "...", sources: [...] }
│   /chat         │
└─────────────────┘
```

---

## 8. AI 파이프라인

### 8.1 LLM 추론 엔진 (vLLM)

```
┌─────────────────────────────────────────────────────────────────┐
│                        vLLM Architecture                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Model: meta-llama/Meta-Llama-3.1-8B-Instruct                   │
│  GPU Memory: 70% utilization (~16GB for 8B model)               │
│  Max Model Length: 4096 tokens                                   │
│                                                                  │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │                    PagedAttention                           ││
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐           ││
│  │  │ Block 1 │ │ Block 2 │ │ Block 3 │ │ Block N │           ││
│  │  │ KV Cache│ │ KV Cache│ │ KV Cache│ │ KV Cache│           ││
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘           ││
│  │                                                             ││
│  │  효과: 메모리 단편화 방지, 2-4배 처리량 향상                ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  API 호환성:                                                     │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │  POST /v1/chat/completions                                  ││
│  │  (OpenAI SDK 그대로 사용 가능)                              ││
│  │                                                             ││
│  │  client = AsyncOpenAI(base_url="http://vllm:8000/v1")      ││
│  │  response = await client.chat.completions.create(...)      ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 8.2 임베딩 엔진 (TEI)

```
┌─────────────────────────────────────────────────────────────────┐
│                   Text Embeddings Inference                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Model: BAAI/bge-m3                                             │
│  Vector Dimension: 1024                                          │
│  Languages: 다국어 지원 (한국어 포함)                            │
│                                                                  │
│  API Endpoints:                                                  │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │  POST /embed                                                ││
│  │  Request:  { "inputs": "텍스트", "truncate": true }         ││
│  │  Response: [[0.12, -0.34, 0.56, ...]]  (1024 floats)        ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  사용 사례:                                                      │
│  ├─ 매뉴얼 인덱싱: 장애 대응 매뉴얼을 벡터화하여 Qdrant 저장   │
│  ├─ 쿼리 임베딩: 사용자 질문을 벡터로 변환                     │
│  └─ 유사도 검색: Cosine 유사도로 Top-K 문서 검색               │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 8.3 로그 템플릿 추출 (Drain3)

```
┌─────────────────────────────────────────────────────────────────┐
│                     Drain3 Algorithm                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  목적: 다양한 로그 메시지를 소수의 템플릿으로 클러스터링        │
│                                                                  │
│  동작 원리:                                                      │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │  입력 로그들:                                               ││
│  │  1. "Connection to 192.168.1.1 failed"                      ││
│  │  2. "Connection to 10.0.0.5 failed"                         ││
│  │  3. "User admin logged in"                                  ││
│  │  4. "User guest logged in"                                  ││
│  │                                                             ││
│  │  출력 템플릿:                                               ││
│  │  Template 1: "Connection to <*> failed"                     ││
│  │  Template 2: "User <*> logged in"                           ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  설정 (drain3.ini):                                             │
│  ├─ similarity_threshold: 0.5 (유사도 기준)                    │
│  ├─ depth: 4 (파싱 트리 깊이)                                  │
│  └─ max_clusters: 1000 (최대 템플릿 수)                        │
│                                                                  │
│  장점:                                                           │
│  ├─ 스트리밍 처리 (온라인 학습)                                │
│  ├─ 메모리 효율적                                              │
│  └─ 정확도 높음 (90%+ on common log formats)                   │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 8.4 이상 탐지 (PyOD)

```
┌─────────────────────────────────────────────────────────────────┐
│                  Isolation Forest (PyOD)                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  알고리즘 원리:                                                  │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │  "이상치는 정상 데이터보다 고립하기 쉽다"                   ││
│  │                                                             ││
│  │  1. 랜덤 특성 선택                                          ││
│  │  2. 랜덤 분할점 선택                                        ││
│  │  3. 재귀적 분할로 트리 생성                                 ││
│  │  4. 평균 경로 길이가 짧을수록 이상치                        ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  설정:                                                           │
│  ├─ contamination: 0.01 (1%를 이상치로 가정)                   │
│  ├─ n_estimators: 100 (앙상블 트리 수)                         │
│  └─ random_state: 42 (재현성)                                  │
│                                                                  │
│  특성 엔지니어링:                                                │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │  현재 구현 (단순화):                                        ││
│  │  - 특성: 분당 템플릿별 로그 발생 횟수                       ││
│  │                                                             ││
│  │  권장 확장:                                                 ││
│  │  - 서비스별 에러 비율                                       ││
│  │  - 응답 시간 분포                                           ││
│  │  - 시간대별 패턴 편차                                       ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 8.5 시스템 프롬프트 (AI 페르소나)

```markdown
# system_prompt.md 구조

┌─────────────────────────────────────────────────────────────────┐
│                    AI 페르소나 설정                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  역할: Lead Site Reliability Engineer (SRE) AI                  │
│  언어: 한국어 (Korean)                                          │
│  톤: 전문적, 분석적, 간결                                       │
│                                                                  │
│  접근 가능 데이터:                                               │
│  ├─ 실시간 서버 로그 (Drain3 파싱)                             │
│  ├─ 과거 장애 데이터베이스 (Qdrant RAG)                        │
│  └─ 이상 점수 (PyOD 메트릭)                                    │
│                                                                  │
│  분석 프레임워크: OODA Loop                                      │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  1. Observe (관찰): 로그 증상 파악                         │ │
│  │  2. Orient (판단): 과거 사례와 비교                        │ │
│  │  3. Decide (결정): 근본 원인 추정                          │ │
│  │  4. Act (행동): CLI 명령어 또는 조치 권고                   │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  출력 템플릿:                                                    │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │  ### 🚨 [Severity] 장애 감지 리포트                        │ │
│  │                                                            │ │
│  │  **1. 현상 분석**                                          │ │
│  │  - 요약: (한 줄)                                           │ │
│  │  - 관련 지표: (이상 점수, 에러 빈도)                       │ │
│  │                                                            │ │
│  │  **2. 예상 원인**                                          │ │
│  │  - (근본 원인 설명)                                        │ │
│  │                                                            │ │
│  │  **3. 조치 권고**                                          │ │
│  │  - [ ] (즉시 실행할 명령어)                                │ │
│  │                                                            │ │
│  │  **4. 예상 영향도**                                        │ │
│  │  - (서비스 중단 가능성)                                    │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 9. API 명세

### 9.1 REST API 엔드포인트

```
┌─────────────────────────────────────────────────────────────────┐
│                      API Endpoints                               │
│                   Base URL: http://localhost:8000                │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │  Health Check                                               ││
│  │  GET /health                                                ││
│  │  Response: { "status": "ok", "version": "1.0.0" }           ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │  Logs API                                                   ││
│  │  GET /api/v1/logs?limit=100&service=auth-service            ││
│  │                                                             ││
│  │  Response:                                                  ││
│  │  [                                                          ││
│  │    {                                                        ││
│  │      "timestamp": "2024-01-01T10:00:00",                    ││
│  │      "level": "ERROR",                                      ││
│  │      "service": "auth-service",                             ││
│  │      "message": "Connection failed"                         ││
│  │    }                                                        ││
│  │  ]                                                          ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │  Stats API                                                  ││
│  │  GET /api/v1/stats/summary                                  ││
│  │                                                             ││
│  │  Response:                                                  ││
│  │  {                                                          ││
│  │    "recent_errors": 5,                                      ││
│  │    "recent_anomalies": 1,                                   ││
│  │    "system_status": "CRITICAL"                              ││
│  │  }                                                          ││
│  │                                                             ││
│  │  GET /api/v1/stats/trend                                    ││
│  │  Response:                                                  ││
│  │  [                                                          ││
│  │    { "time": "2024-01-01T10:00:00", "count": 3 },           ││
│  │    { "time": "2024-01-01T11:00:00", "count": 0 }            ││
│  │  ]                                                          ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │  Analysis API                                               ││
│  │  POST /api/v1/analysis/trigger                              ││
│  │                                                             ││
│  │  Request:                                                   ││
│  │  {                                                          ││
│  │    "template_id": 42,                                       ││
│  │    "timestamp": "2024-01-01T10:00:00",                      ││
│  │    "details": "High error rate detected"                    ││
│  │  }                                                          ││
│  │                                                             ││
│  │  Response:                                                  ││
│  │  {                                                          ││
│  │    "analysis": "### 장애 분석 결과\n..."                    ││
│  │  }                                                          ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │  Chat API                                                   ││
│  │  POST /api/v1/chat                                          ││
│  │                                                             ││
│  │  Request:                                                   ││
│  │  {                                                          ││
│  │    "message": "최근 API 장애 원인은?",                      ││
│  │    "history": [                                             ││
│  │      { "role": "user", "content": "이전 질문" },            ││
│  │      { "role": "assistant", "content": "이전 답변" }        ││
│  │    ]                                                        ││
│  │  }                                                          ││
│  │                                                             ││
│  │  Response:                                                  ││
│  │  {                                                          ││
│  │    "response": "### 분석 결과\n메모리 누수가...",           ││
│  │    "sources": ["매뉴얼: 메모리 누수 대응법"]                ││
│  │  }                                                          ││
│  │                                                             ││
│  │  GET /api/v1/chat/health                                    ││
│  │  Response: { "status": "ok", "service": "chat" }            ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 9.2 외부 서비스 API

```
┌─────────────────────────────────────────────────────────────────┐
│                    External Service APIs                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  vLLM (http://vllm:8000 or http://localhost:8000)               │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │  POST /v1/chat/completions                                  ││
│  │  (OpenAI API 호환)                                          ││
│  │                                                             ││
│  │  GET /health                                                ││
│  │  GET /v1/models                                             ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  TEI (http://tei:8080 or http://localhost:8080)                 │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │  POST /embed                                                ││
│  │  Request: { "inputs": "text" | ["text1", "text2"] }         ││
│  │                                                             ││
│  │  GET /health                                                ││
│  │  GET /info                                                  ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  Qdrant (http://qdrant:6333 or http://localhost:6333)           │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │  GET /collections                                           ││
│  │  PUT /collections/{name}                                    ││
│  │  POST /collections/{name}/points/search                     ││
│  │  PUT /collections/{name}/points                             ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  ClickHouse (http://clickhouse:8123 or http://localhost:8123)   │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │  GET /ping                                                  ││
│  │  POST / (SQL query in body)                                 ││
│  │  Web UI: /play                                              ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  Redpanda (http://redpanda:8082 or http://localhost:8082)       │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │  Kafka API: port 29092 (외부), 9092 (내부)                  ││
│  │  REST Proxy: port 8082                                      ││
│  │  Admin API: port 9644                                       ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 10. 배포 구성

### 10.1 개발 환경 실행

```batch
# Windows 환경 (.bat 스크립트 사용)

# 1. 초기 설정
setup.bat
# - Python venv 생성
# - pip install -r requirements.txt
# - npm install (frontend)

# 2. 인프라 서비스 시작
docker-compose up -d
# 시작 순서: redpanda → clickhouse → qdrant → vector → backend → frontend

# 3. AI 서비스 시작 (GPU 필요)
docker-compose -f docker-compose.ai.yml up -d
# 시작 순서: vllm, tei (병렬)

# 4. 개발 서버 실행 (hot-reload)
run_app.bat
# - Backend: uvicorn main:app --reload (port 8000)
# - Frontend: npm run dev (port 3000)
```

### 10.2 프로덕션 배포

```
┌─────────────────────────────────────────────────────────────────┐
│                   Production Deployment                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  권장 사양:                                                      │
│  ├─ CPU: 16 cores (Backend 워커용)                              │
│  ├─ RAM: 64GB (ClickHouse, Redpanda 버퍼)                       │
│  ├─ GPU: NVIDIA RTX 3090/4090 또는 A100 (24GB+ VRAM)            │
│  ├─ Storage: 1TB+ SSD (로그 저장용)                             │
│  └─ Network: 10Gbps (로그 수집 대역폭)                          │
│                                                                  │
│  스케일링 전략:                                                  │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │  수평 확장 가능:                                            ││
│  │  ├─ ingest_consumer: Kafka Consumer Group으로 다중 인스턴스││
│  │  ├─ backend: FastAPI 다중 워커 (uvicorn --workers N)        ││
│  │  └─ frontend: CDN + 다중 인스턴스                           ││
│  │                                                             ││
│  │  수직 확장 필요:                                            ││
│  │  ├─ vLLM: 더 큰 GPU 또는 다중 GPU                           ││
│  │  ├─ ClickHouse: 샤딩으로 분산 가능                          ││
│  │  └─ Qdrant: 클러스터 모드 지원                              ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 10.3 환경 변수

```env
# .env 파일 예시

# HuggingFace (모델 다운로드용)
HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxx

# LLM 설정
LLM_MODEL_NAME=meta-llama/Meta-Llama-3.1-8B-Instruct
LLM_PROVIDER=local        # "local" (vLLM) 또는 "openai"
LLM_API_KEY=sk-xxx        # OpenAI 사용 시 필요

# 인프라 엔드포인트 (Docker 내부망)
REDPANDA_BROKER=redpanda:9092
CLICKHOUSE_HOST=clickhouse
CLICKHOUSE_PORT=8123
QDRANT_HOST=qdrant
QDRANT_PORT=6333
VLLM_URL=http://vllm:8000/v1
TEI_URL=http://tei:8080

# 알림 (선택)
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/xxx/yyy/zzz

# Frontend (Next.js)
NEXT_PUBLIC_API_URL=http://localhost:8000
```

---

## 11. 보안 고려사항

### 11.1 현재 상태 (개발 환경)

```
┌─────────────────────────────────────────────────────────────────┐
│                  Current Security Status                         │
│                     (Development Mode)                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ⚠️ 개발 환경 설정 (프로덕션에서 변경 필요):                     │
│                                                                  │
│  ├─ CORS: allow_origins=["*"]  → 특정 도메인으로 제한           │
│  ├─ ClickHouse: 무암호 접속    → users.xml에서 암호 설정        │
│  ├─ Qdrant: 인증 없음          → API Key 설정                   │
│  ├─ Redpanda: 인증 없음        → SASL/TLS 설정                  │
│  └─ vLLM: api_key="EMPTY"      → 실제 API Key 설정              │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 11.2 프로덕션 보안 체크리스트

```
┌─────────────────────────────────────────────────────────────────┐
│                Production Security Checklist                     │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  [ ] 네트워크 보안                                               │
│      ├─ [ ] 내부 서비스 포트 방화벽 차단 (6333, 8123, 9092 등)  │
│      ├─ [ ] HTTPS 적용 (Let's Encrypt 또는 내부 CA)             │
│      └─ [ ] VPN 또는 Private Network 구성                       │
│                                                                  │
│  [ ] 인증 및 권한                                                │
│      ├─ [ ] FastAPI에 JWT 인증 추가                             │
│      ├─ [ ] 역할 기반 접근 제어 (RBAC)                          │
│      └─ [ ] API Rate Limiting                                   │
│                                                                  │
│  [ ] 데이터 보안                                                 │
│      ├─ [ ] 로그 내 민감 정보 마스킹 (PII, 비밀번호 등)         │
│      ├─ [ ] ClickHouse 암호화 at-rest                           │
│      └─ [ ] 백업 및 복구 계획                                   │
│                                                                  │
│  [ ] AI 보안                                                     │
│      ├─ [ ] Prompt Injection 방어                               │
│      ├─ [ ] 출력 필터링 (민감 정보 노출 방지)                   │
│      └─ [ ] 모델 버전 관리 및 감사 로그                         │
│                                                                  │
│  [ ] 운영 보안                                                   │
│      ├─ [ ] 환경 변수 암호화 (HashiCorp Vault 등)               │
│      ├─ [ ] 컨테이너 이미지 스캔                                │
│      └─ [ ] 보안 업데이트 자동화                                │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 12. 확장성 및 성능

### 12.1 현재 성능 특성

```
┌─────────────────────────────────────────────────────────────────┐
│                   Performance Characteristics                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  로그 수집 (Vector → Redpanda):                                  │
│  ├─ 처리량: ~10,000 logs/sec (단일 인스턴스)                    │
│  └─ 지연시간: < 100ms (수집 → 저장)                             │
│                                                                  │
│  로그 저장 (ClickHouse):                                         │
│  ├─ 삽입: ~500,000 rows/sec (배치 모드)                         │
│  ├─ 쿼리: < 100ms (최근 1시간 집계)                             │
│  └─ 압축률: ~10:1 (원본 대비)                                   │
│                                                                  │
│  AI 추론 (vLLM + Llama 3.1-8B):                                  │
│  ├─ 처리량: ~20 req/sec (RTX 4090 기준)                         │
│  ├─ 지연시간: 2-5초 (500 토큰 생성)                             │
│  └─ GPU 메모리: ~16GB                                           │
│                                                                  │
│  임베딩 (TEI + bge-m3):                                          │
│  ├─ 처리량: ~100 req/sec                                        │
│  └─ 지연시간: < 50ms                                            │
│                                                                  │
│  벡터 검색 (Qdrant):                                             │
│  ├─ 검색: < 10ms (100만 벡터 기준)                              │
│  └─ 인덱싱: 실시간                                              │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 12.2 병목 지점 및 최적화

```
┌─────────────────────────────────────────────────────────────────┐
│                 Bottlenecks & Optimizations                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  병목 1: LLM 추론 속도                                           │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │  현재: 단일 GPU, 순차 처리                                  ││
│  │                                                             ││
│  │  최적화 방안:                                               ││
│  │  ├─ Continuous Batching 활성화 (vLLM 기본)                  ││
│  │  ├─ 다중 GPU 분산 (Tensor Parallelism)                      ││
│  │  ├─ 더 작은 모델 사용 (Llama 3.1-3B, Phi-3-mini)            ││
│  │  └─ 캐싱: 자주 묻는 질문 응답 캐시                          ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  병목 2: 로그 수집 스파이크                                      │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │  현재: 단일 Consumer, 100개 배치                            ││
│  │                                                             ││
│  │  최적화 방안:                                               ││
│  │  ├─ Consumer Group으로 다중 워커                            ││
│  │  ├─ 배치 크기 증가 (100 → 1000)                             ││
│  │  ├─ Redpanda 파티션 증가                                    ││
│  │  └─ 백프레셔 처리 (버퍼 오버플로우 방지)                    ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
│  병목 3: 이상 탐지 학습 시간                                     │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │  현재: 매번 전체 데이터로 재학습                            ││
│  │                                                             ││
│  │  최적화 방안:                                               ││
│  │  ├─ 증분 학습 (Incremental Learning)                        ││
│  │  ├─ 모델 캐싱 (훈련된 모델 저장/로드)                       ││
│  │  └─ 특성 엔지니어링 최적화                                  ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 12.3 확장 로드맵

```
┌─────────────────────────────────────────────────────────────────┐
│                    Scaling Roadmap                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Phase 1: 수직 확장 (현재)                                       │
│  ├─ 단일 서버에서 모든 서비스 실행                              │
│  ├─ GPU 1장 (16-24GB VRAM)                                      │
│  └─ 처리량: ~1,000 logs/sec                                     │
│                                                                  │
│  Phase 2: 서비스 분리                                            │
│  ├─ AI 서비스 전용 GPU 서버 분리                                │
│  ├─ ClickHouse 전용 스토리지 서버                               │
│  └─ 처리량: ~10,000 logs/sec                                    │
│                                                                  │
│  Phase 3: 수평 확장                                              │
│  ├─ Kubernetes 배포                                             │
│  ├─ ClickHouse 클러스터 (샤딩 + 레플리케이션)                   │
│  ├─ Qdrant 클러스터                                             │
│  ├─ Consumer 워커 오토스케일링                                  │
│  └─ 처리량: ~100,000 logs/sec                                   │
│                                                                  │
│  Phase 4: 멀티 리전                                              │
│  ├─ 지역별 로그 수집 → 중앙 분석                                │
│  ├─ 엣지 추론 (경량 모델)                                       │
│  └─ 글로벌 처리량: ~1,000,000 logs/sec                          │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 부록

### A. 용어 사전

| 용어 | 설명 |
|------|------|
| **Drain3** | 로그 템플릿 추출 알고리즘. 유사한 로그를 클러스터링하여 패턴화 |
| **PyOD** | Python Outlier Detection. 다양한 이상 탐지 알고리즘 라이브러리 |
| **RAG** | Retrieval-Augmented Generation. 검색 결과를 LLM에 제공하는 기법 |
| **vLLM** | 고성능 LLM 추론 엔진. PagedAttention으로 GPU 메모리 효율화 |
| **TEI** | Text Embeddings Inference. HuggingFace의 임베딩 추론 서버 |
| **LangGraph** | LangChain의 그래프 기반 에이전트 워크플로우 라이브러리 |
| **OODA Loop** | Observe-Orient-Decide-Act. 군사 전략에서 유래한 의사결정 프레임워크 |
| **MTTR** | Mean Time To Recovery. 장애 복구까지 걸리는 평균 시간 |

### B. 참고 자료

- [Drain3 GitHub](https://github.com/IBM/Drain3)
- [PyOD Documentation](https://pyod.readthedocs.io/)
- [vLLM Documentation](https://docs.vllm.ai/)
- [LangGraph Documentation](https://python.langchain.com/docs/langgraph)
- [ClickHouse Documentation](https://clickhouse.com/docs)
- [Qdrant Documentation](https://qdrant.tech/documentation/)
- [Redpanda Documentation](https://docs.redpanda.com/)

### C. 문서 변경 이력

| 버전 | 날짜 | 변경 내용 | 작성자 |
|------|------|----------|--------|
| 1.0.0 | 2026-01-03 | 초기 문서 작성 | Claude AI |

---

> **Note**: 이 문서는 LogAi 프로젝트의 현재 구현 상태를 기반으로 작성되었습니다.
> 프로젝트가 발전함에 따라 지속적으로 업데이트가 필요합니다.
